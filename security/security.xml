<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- Regla global para permitir acceso a todos -->
        <record id="attendance_realtime_all_rule" model="ir.rule">
            <field name="name">Acceso Global a Asistencias</field>
            <field name="model_id" ref="model_dityc_attendance_realtime_view"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="global" eval="True"/>
        </record>
    </data>
</odoo> 